const path = require('path');

module.exports = {
  config: path.resolve('src', 'config', 'connection.ts'),
  'migrations-path': path.resolve("src", 'migrations'),
  'models-path': path.resolve('src', 'models'),
};